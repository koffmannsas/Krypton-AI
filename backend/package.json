{
  "name": "krypton-ai-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "esbuild src/server.ts --platform=node --format=esm --bundle --packages=external --outdir=dist",
    "start": "NODE_ENV=production node dist/server.js"
  },
  "dependencies": {
    "@google/genai": "^1.50.1",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "firebase-admin": "^13.8.0",
    "esbuild": "^0.28.0",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^8.0.10"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.14.0"
  }
}
